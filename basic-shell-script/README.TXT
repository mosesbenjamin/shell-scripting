# Executable for owner only
chmod u+x filename

# Executable for everyone
chmod a+x filename

# Remove executable permission
chmod a-x filename

# Specifies which interpreter should run the code
#!/bin/bash

# When unsure where shell is located
#!/usr/bin/env zsh 
    OR
#!/usr/bin/env bash
